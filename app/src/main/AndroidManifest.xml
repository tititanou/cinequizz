<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tania.cinequizz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/cine_icone_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/cine_icone_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EndActivity"></activity>
        <activity android:name=".QuestionsListActivity" />
        <activity android:name=".ZoomActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AboutActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>